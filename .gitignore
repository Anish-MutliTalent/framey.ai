node_modules
dist
.env
backend/.env
backend/academia.db
backend/__pycache__
backend/.venv
backend/.idea
*.pyc

# The default library ships in the repo (backend/library_assets/) so deployed
# servers see it; edit it locally and commit. (Admin-uploaded content, when
# added as a feature, should go in a separate gitignored folder.)
